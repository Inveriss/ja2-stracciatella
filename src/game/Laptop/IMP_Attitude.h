#ifndef IMP_ATTITUDE_H
#define IMP_ATTITUDE_H

// The "I.M.P. Attitudes" page, shown right after the skill traits page
// (only used with the pick_skills_directly policy). The selection is stored
// directly in iAttitude.
void EnterIMPAttitude(void);
void RenderIMPAttitude(void);
void ExitIMPAttitude(void);
void HandleIMPAttitude(void);

#endif
