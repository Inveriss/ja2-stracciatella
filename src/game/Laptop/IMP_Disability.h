#ifndef IMP_DISABILITY_H
#define IMP_DISABILITY_H

// The "I.M.P. Disabilities" page, shown right after the attitudes page
// (only used with the pick_skills_directly policy). The selection is stored
// directly in iPersonality.
void EnterIMPDisability(void);
void RenderIMPDisability(void);
void ExitIMPDisability(void);
void HandleIMPDisability(void);

#endif
